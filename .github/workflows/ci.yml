# SPDX-License-Identifier: AGPL-3.0-or-later
# Copyright (C) 2026 The Fisher Slopworks Co

# Everything a pull request runs. Building and shipping the image lives in
# release.yml, which pull requests don't trigger at all — see check.yml for why
# that split exists rather than an `if:` on those jobs.
name: CI

on:
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check:
    uses: ./.github/workflows/check.yml
